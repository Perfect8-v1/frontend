# ═══════════════════════════════════════════════════════════════════════════
# Perfect8 Frontend - Automatisk Deployment
# ═══════════════════════════════════════════════════════════════════════════
# 
# Denna fil gör följande:
#   1. Triggas när kod pushas till 'main'
#   2. Bygger Flutter web-app
#   3. Kopierar till servern via SSH
#
# Secrets som behövs (Settings → Secrets → Actions):
#   - SSH_PRIVATE_KEY : Privat SSH-nyckel
#   - SSH_HOST        : p8.rantila.com
#   - SSH_USER        : magnus
#
# ═══════════════════════════════════════════════════════════════════════════

name: Deploy Flutter Web

# ─────────────────────────────────────────────────────────────────────────────
# NÄR ska workflow köras?
# ─────────────────────────────────────────────────────────────────────────────
on:
  # Vid push till main
  push:
    branches:
      - main
  
  # Manuell trigger (kan köras från GitHub UI)
  workflow_dispatch:

# ─────────────────────────────────────────────────────────────────────────────
# JOBB
# ─────────────────────────────────────────────────────────────────────────────
jobs:
  build-and-deploy:
    name: Build and Deploy
    runs-on: ubuntu-latest
    
    steps:
      # ─────────────────────────────────────────────────────────────────────
      # STEG 1: Hämta koden från repot
      # ─────────────────────────────────────────────────────────────────────
      - name: 📥 Checkout code
        uses: actions/checkout@v4
      
      # ─────────────────────────────────────────────────────────────────────
      # STEG 2: Installera Flutter
      # ─────────────────────────────────────────────────────────────────────
      - name: 🐦 Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.24.0'
          channel: 'stable'
      
      # ─────────────────────────────────────────────────────────────────────
      # STEG 3: Hämta dependencies
      # ─────────────────────────────────────────────────────────────────────
      - name: 📦 Get dependencies
        run: flutter pub get
      
      # ─────────────────────────────────────────────────────────────────────
      # STEG 4: Bygg för web
      # ─────────────────────────────────────────────────────────────────────
      - name: 🔨 Build web
        run: flutter build web --release
      
      # ─────────────────────────────────────────────────────────────────────
      # STEG 5: Sätt upp SSH
      # ─────────────────────────────────────────────────────────────────────
      - name: 🔑 Setup SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-keyscan -H ${{ secrets.SSH_HOST }} >> ~/.ssh/known_hosts
      
      # ─────────────────────────────────────────────────────────────────────
      # STEG 6: Kopiera till servern
      # ─────────────────────────────────────────────────────────────────────
      - name: 🚀 Deploy to server
        run: |
          rsync -avz --delete \
            -e "ssh -i ~/.ssh/deploy_key" \
            build/web/ \
            ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }}:/var/www/perfect8/
      
      # ─────────────────────────────────────────────────────────────────────
      # STEG 7: Bekräfta deployment
      # ─────────────────────────────────────────────────────────────────────
      - name: ✅ Deployment complete
        run: |
          echo "════════════════════════════════════════════════════════"
          echo "✅ Deployment successful!"
          echo "🌐 Site: http://p8.rantila.com"
          echo "📅 Time: $(date)"
          echo "════════════════════════════════════════════════════════"
